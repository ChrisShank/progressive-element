{
  "name": "progressive-element",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "pnpm build && vite preview"
  },
  "devDependencies": {
    "typescript": "~5.6.0",
    "vite": "^4.4.0"
  }
}
