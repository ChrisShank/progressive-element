{
  "name": "progressive-element",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "pnpm build && vite preview"
  },
  "dependencies": {
    "lit": "^2.7.5"
  },
  "devDependencies": {
    "typescript": "^5.1.6",
    "vite": "^4.4.0"
  }
}
